# scripts/sync_from_airtable.py / scripts/generate_images.py が読む環境変数。
# コピーして .env という名前で保存し、実際の値を入れる（.env はgit管理しない）。
AIRTABLE_API_KEY=
AIRTABLE_BASE_ID=
# 本番ドメインが決まったら設定する（sitemap.xmlの絶対URL生成に使う）
SITE_URL=
# fal.ai のAPIキー（画像自動生成用。 https://fal.ai で取得）
FAL_KEY=
