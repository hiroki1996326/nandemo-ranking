# ランキン！ — 設計書

> 「事実」としてのランキングを発表するサイト。みんなのランキング（投稿型）ではなく、
> 漁獲量・輸出額・人口など、統計・記録に基づくランキングを記事として届ける。
>
> **サイト名: ランキン！ / 本番ドメイン: https://rankin-q.com**

最終更新: 2026-07-10

---

## 1. コンセプト

- **対象**: 数値で順位が付けられる事実全般（経済・産業、人口・社会、地理・自然、スポーツ・興行 など）
- **投稿型ではない**: ユーザー投票・ユーザー投稿のランキングは扱わない。出典のある一次データのみ
- **扱わないもの**: 日次で変わるもの（株価・為替など）。更新の速さでは戦わない
- **差別化の核**: 「同じお題を年ごとに並べて、変化が判る」こと。単発の「2025年TOP10」記事の寄せ集めではなく、
  前年比・順位変動を機械的に算出して見せる（詳細は §3）

---

## 2. お題の2タイプ

| タイプ | 例 | 期間軸 |
|---|---|---|
| **推移モノ**（サイトの主役） | 漁獲量、輸出額、人口、興行収入 | あり（年次/月次/シーズンごと）。前年比・順位変動が主役 |
| **staticモノ**（フックとして少量） | 日本一長い川、世界一高い山、都道府県の面積 | なし。単発の事実として1セットのみ |

---

## 3. データモデル

正規化した2テーブル構成（Airtableの2ベース、または将来DBに載せ替えても同じ形）。

### topics（お題のメタ情報）— 1行 = 1お題

| フィールド | 型 | 説明 |
|---|---|---|
| id | 単一行テキスト（主キー） | URLスラッグ。例: `gyokakuryo-todofuken` |
| title | 単一行テキスト | 記事タイトル。**必ず「ランキング」を含める** |
| category | 単一選択（keizai/jinko/chiri/sports） | カテゴリ |
| unit | 単一行テキスト | 単位（トン・km・km²・m・億円 等） |
| source | 単一行テキスト | 出典名 |
| source_url | URL | 出典リンク |
| update_frequency | 単一選択（static/yearly/monthly/irregular） | 更新頻度。日次は扱わない |
| updated_at | 日付 | 最終更新日 |
| lead | 長文テキスト | リード文（1〜2文の要約） |
| commentary | 長文テキスト | 本文（表の前に置く短い解説） |
| analysis_heading | 単一行テキスト | 考察セクションの見出し。**「考察」という一言ではなく、実際に考察している内容を要約した見出しにする**（例:「北海道が突出、上位10県のうち5県が東北地方」） |
| analysis | 長文テキスト | 考察本文。段落は空行（`\n\n`）区切りで複数入力し、同期スクリプトが配列に分割する。**事実・数値のみ、主観語（「〜だろう」「注意が必要」等）を避ける** |

### 事実選定・ライティングの原則（Airtable入力時も厳守）

- **タイトルは必ず「ランキング」を含む**
- **考察（analysis）は主観を排し、数値と順位差のみで構成する**（誰目線でもない書き方）
- 絵文字は使わない（カテゴリ・タグ・見出し等すべて）

### entities（実体マスタ）— 1行 = 1つの概念（都道府県・国・山・食べ物…すべて）

「新潟県」「そうめん」のような、複数のランキングに繰り返し登場しうる項目を一意に管理するテーブル。
同じ実体を別記事で使うたびに新しく作らず、既存のEntitiesから探して再利用する（表記ゆれ厳禁。「アメリカ」と「アメリカ合衆国」を別レコードにしない）。

| フィールド | 型 | 説明 |
|---|---|---|
| id | 単一行テキスト（主キー） | スラッグ。例: `niigata`, `somen` |
| name | 単一行テキスト | 表示名。例: `新潟県`, `そうめん` |
| type | 単一選択（prefecture/country/mountain/lake/river/building/movie/religion/language/food/ocean/continent/other） | 種別。新しい種別が必要になったら追加してよい |
| image_url | URL | 画像のURL。**Wikimedia Commonsのパブリックドメイン/CC0/CCライセンス画像のみ**（著作権リスクのある画像は絶対に貼らない） |
| image_credit | 単一行テキスト | 画像のクレジット表記（CCライセンスで表記義務がある場合に記入。パブリックドメインなら空欄可） |

- 画像は同期スクリプトが`image_url`からダウンロードして`public/images/entities/`配下に保存し、サイトはそのローカルパスを参照する（Airtableの添付ファイルURLは将来変わる可能性があるため直接参照しない）
- 画像は「実体の詳細ページ（例: `/entity/niigata`）」と、記事の順位表・表彰台の各項目の**小さいサムネ**（実体に画像がある項目のみ、名前の横に表示）に使う。実体画像は取得元により実写・国旗などになるため、大きく前面には出さず、あくまで補助的な小サムネにとどめる

### entries（各期間・各順位のデータ）— 1行 = 1トピックの1期間の1件

| フィールド | 型 | 説明 |
|---|---|---|
| topic | Topicsへのリンク | 紐づくお題 |
| entity | Entitiesへのリンク | 項目（都道府県・国など）。新規の実体は、まずEntitiesに1件作ってからリンクする |
| period | 単一行テキスト | 年など。staticなお題は空欄 |
| value | 数値 | 順位算出のもとになる数値。**rankは保存しない**（表示側でvalue降順に自動算出） |

例：

| topic | entity | period | value |
|---|---|---|---|
| gyokakuryo-todofuken | niigata | 2024 | 165000 |
| gyokakuryo-todofuken | niigata | 2025 | 190000 |
| nihon-nagai-kawa | shinano-river | (空) | 367 |

- staticなお題は `period` を空にして1セットのみ持つ
- **1トピックあたりの件数に上限はない**。TOP10を超えるデータ（都道府県全47、世界の国50カ国など）も歓迎で、サイト側は11位以降を「11位以降のランキング」として折りたたみ表示する
- 「都道府県×年代別人口」のような2軸以上のお題は**今は対応しない**（YAGNI。具体例が出てから拡張を検討）

### 派生指標（表示側で自動算出、人間は入力しない）

- **順位変動**: 前期間との `rank` 差分（↑2 / → / ↓1）
- **増減率**: `(今期value − 前期value) / 前期value × 100`
- **メタランキング（将来）**: 「今年いちばん変動が大きかったお題TOP」のような横断ランキング

---

## 4. サイトマップ

```
トップ /                    今日時点の注目ランキング（各カテゴリの代表トピックを抜粋）
カテゴリ一覧 /category/xxx        カテゴリ内の全トピック一覧
個別トピックページ /topic/xxx      最新順位表 + 推移グラフ + 前年比 + 解説文 + 出典リンク
実体詳細ページ /entity/xxx        都道府県・国などが登場する全ランキングへの内部リンク一覧
実体type一覧 /entity-type/xxx    同じtype（都道府県・国など）の実体を並べた一覧
このサイトについて /about        データの扱い方・出典方針・投稿型サイトとの違い
```

- 個別トピックページが最も重要。表 + 折れ線グラフ（推移モノのみ）+ 解説文（なぜ変わったか）+ 出典
- **実体（Entities）はカテゴリ・記事に従属しない横断的な存在**。パンくずは「ホーム › 実体type一覧 › 実体名」とし、記事のパンくずとは別系統にする（1つの実体が複数カテゴリ・複数記事から参照されるため、記事側のパンくずに組み込むと1つのURLに複数の正しいパンくずができてしまい矛盾するため）
- 検索・全トピック横断ページはMVP後（データが増えてから）でよい

---

## 5. カテゴリ・MVPスコープ

4カテゴリ、各3〜5本、計15本前後で開始。

- **経済・産業**（推移モノ中心）: 国別輸出額、都道府県別漁獲量、都道府県別米収穫量
- **人口・社会**（推移モノ）: 都道府県別人口、市区町村別人口増減率
- **地理・自然**（staticモノ、フック用）: 日本一長い川、世界一高い山、都道府県別面積
- **スポーツ・興行**（推移モノ）: 年間興行収入、プロ野球観客動員数

---

## 6. 技術構成（[Internet meme](../Internet%20meme) と揃える）

- **静的サイト**: フレームワークなし、素のHTML/CSS/JS。`public/` 配下のみ公開
- **ホスティング**: Cloudflare Workers（assetsバインディング、クリーンURL）
- **データ管理**: Airtable（Topics / Entries の2テーブル、リンク済み）→ 同期スクリプト（`scripts/sync_from_airtable.py`）で `public/data/ranking-data.js`（`window.RANKING_DATA` を代入する形。fetch不要でCORSの心配がない）に書き出し
- **自動化**: GitHub Actions（Airtable同期 → 派生指標の再計算 → 再デプロイ）
- リポジトリはprivate、dev → プレビュー → main の3段運用（ミームサイトと同じ）

**ミームサイトとのシナジー**:
- 同じ技術パターン（Airtable → JSON → 静的配信）を使い回せる
- 同期スクリプト（`scripts/sync_from_airtable.py` 相当）はほぼ流用可能
- 将来的にトップページ間の相互送客も検討可能（別サイトとして独立運用しつつリンク）

---

## 7. 著作権・出典方針

- 一次データは統計局・省庁・公的機関のものを優先し、必ず出典元へのリンクを明記
- 二次加工（グラフ化・解説文）は自作。転載はしない
- **画像（Entities.image_url）はWikimedia Commonsのパブリックドメイン/CC0/CCライセンス画像のみ使用可**。Google画像検索などから出典不明の画像を貼らない。CCライセンスで表記義務がある場合は`image_credit`に記入し、サイト上にも表示する
- **国（type=country）の画像は例外的にflagcdn.com（flag-icons、MIT License）の国旗を使う**（`scripts/fetch_country_flags.py`）。国コードで一意に決まり、Wikipedia記事マッチングより確実で画質もシャープなため
- **1〜3位の王冠アイコン（`public/images/icons/rank-crown-*.webp`）はことのはわーくす（ https://kotonohaworks.com/free-icons/oukan/ ）の「ランキング王冠」素材を使用**。商用利用可・クレジット表記不要・改変可（2026-07-12確認）
- **記事サムネイル（`public/images/topics/*.webp`）はFlux(fal.ai)でAI生成する**（`scripts/generate_topic_thumbnails.py`）。背景生成プロンプトは英語のカテゴリ・被写体説明のみで構成し、日本語のタイトル文字列は一切含めない（含めると和風/アニメ調に偏る問題を確認済み）。タイトル文字はPillowで背景と別に合成する

---

## 8. ロードマップ

- **Phase 0（完了）**: コンセプト・データモデル・カテゴリ確定 ← いまここ
- **Phase 1**: Airtableで15本ぶんデータ投入 → 同期スクリプト → 静的サイトで公開（トップ+個別ページ）
- **Phase 2**: カテゴリページ、推移グラフ、前年比・順位変動の自動算出
- **Phase 3**: メタランキング（変動が大きいお題TOP）、検索・横断ページ
- **Phase 4**: カテゴリ追加、他サイトとの相互送客

---

## 9. 未決事項

- ~~サイト名・ドメイン~~（決定済み: サイト名「ランキン！」／ドメイン `rankin-q.com`。SITE_URL・sitemap.xml・robots.txt・OGP・canonicalは設定済み）
- ビジュアルデザインの方向性
- 具体的な15本の最終選定・データソースの当たり
- 2軸以上のお題への対応方針（必要になったら再検討）

## 10. 新しい記事を追加するときの運用ルール（Entities重複防止）

新しい記事のentryを追加する際は、以下の手順を守る（「新潟県」「アメリカ」のような実体を記事ごとに別レコードとして作ってしまう事故を防ぐため）。

1. entryを追加する前に、Airtableの`Entities`テーブルでその項目名を検索する
2. 既に同じ実体があれば、それにリンクする（新規作成しない）
3. なければ`Entities`に1件だけ新規作成してからリンクする
4. 表記ゆれ（「アメリカ」と「アメリカ合衆国」、「新潟県」と「新潟」等）で別レコードを作らないよう、迷ったら`Entities`を`type`列でソートして似た名前が既にないか確認する

## 11. SEO対応の方針

- **今やっていること（軽量対応）**: ルート遷移時にJSで `document.title` / meta description / canonical / OGPタグを動的に書き換え（[app.js](../public/app.js) の `setMeta()`）。`sitemap.xml` / `robots.txt` は同期スクリプトが自動生成
- **まだやっていないこと**: 記事ごとの静的HTML事前生成（prerendering）。現状はSPAでJSが中身を描画するため、JSを実行しないSNSシェアボット（X/LINE等）には正しいOGPが見えない。Googleのクローラーは概ねJSを実行するため検索インデックスへの影響は小さい
- **本実装フェーズでやること**: 記事数が揃いSNS流入を狙う段階になったら、同期スクリプト実行時に記事ごとの静的HTML（`public/topic/<id>/index.html` 等）を事前生成する方式に切り替える
