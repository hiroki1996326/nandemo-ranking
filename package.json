{
  "name": "nandemo-ranking",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:preview": "wrangler deploy --env preview",
    "sync": "python scripts/sync_from_airtable.py"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
